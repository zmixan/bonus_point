#include <bits/stdc++.h>
using namespace std;

int main() {
    int a;
    cin >> a;
    cout << "The next number for the number " << a << " is " << (a + 1) << '.' << endl << "The previous number for the number " << a << " is " << (a - 1) << '.';
}