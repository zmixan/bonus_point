#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, s, d, m;
    cin >> a >> s >> d >> m;
    if (a == d || s == m)
        cout << "YES";
    else
        cout << "NO";
    return 0;
    }