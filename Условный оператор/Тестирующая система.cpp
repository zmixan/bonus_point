#include <bits/stdc++.h>
using namespace std;

int main() {
    int a, s;
    cin >> a >> s;
    if ((a == s) || (a != 1 && s != 1))
        cout << "YES";
        else
        cout << "NO";
    return 0;
    }